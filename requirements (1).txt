# 数据处理与机器学习基座
pandas>=2.2.0
pyarrow>=15.0.0
scikit-learn>=1.4.0
numpy>=1.26.0

# 深度学习与大模型核心组件
transformers>=4.40.0  # Qwen2/2.5等新模型必须使用 4.40 以上版本
datasets>=2.19.0
accelerate>=0.30.0    # 必须安装，用于大模型的设备映射 (device_map="auto")
sentencepiece>=0.2.0  # 许多模型分词器依赖的底层库

# 辅助工具
tqdm>=4.66.0